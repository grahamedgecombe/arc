* Add locking to the virtual memory manager:
   - global lock for the higher half
   - per address-space lock for the lower half

* Delete unused pml3, pml2 and pml1 tables after unmapping pages

* Move as much as possible out of .h files (e.g. structs) for better
  abstraction

